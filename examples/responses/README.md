# Response Examples

Add sanitized response samples here when endpoints are validated against a real ServiceNow instance.

Do not commit customer data, personal data, tokens, tenant-specific secrets, or non-public configuration dumps.

